<?xml version="1.0"?>
<!DOCTYPE modification SYSTEM "http://www.simplemachines.org/xml/modification">
<modification xmlns="http://www.simplemachines.org/xml/modification" xmlns:smf="http://www.simplemachines.org/">
	<id>shawnb61:WARTT</id>
	<version>1.0.1</version>
	<file name="$boarddir/index.php">
		<operation>
			<search position="replace"><![CDATA[// Start the session. (assuming it hasn't already been.)
loadSession();

]]></search>
			<add><![CDATA[// WARTT threshold checks - do before session gets started...
if (!empty($modSettings['wartt_enabled']) && file_exists($warttfile = $sourcedir . '/WARTT.php'))
{
	require_once $warttfile;
	wartt_check_thresholds();
}

// Start the session. (assuming it hasn't already been.)
loadSession();

]]></add>
		</operation>
	</file>
</modification>
